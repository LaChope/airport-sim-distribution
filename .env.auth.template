# This file configures the Keycloak authentication service.
# The username for the initial Keycloak admin user.
KC_BOOTSTRAP_ADMIN_USERNAME=admin

# The password for the initial Keycloak admin user.
# IMPORTANT: Change this to a strong, unique password for production.
KC_BOOTSTRAP_ADMIN_PASSWORD=your_keycloak_admin_password

# The relative path for the Keycloak server.
KC_HTTP_RELATIVE_PATH=/services/auth

# The database vendor (using 'dev-file' for development/testing).
KC_DB=dev-file

# Enable the HTTP listener.
KC_HTTP_ENABLED=true

# The public-facing hostname of your application.
KC_HOSTNAME=airport-sim.your-domain.com

# The full public URL to the Keycloak service.
KC_HOSTNAME_URL=https://airport-sim.your-domain.com/services/auth

# Set to 'true' if your service is behind an HTTPS-terminating reverse proxy.
KC_HOSTNAME_STRICT_HTTPS=true

# Set to 'xforwarded' when running behind a reverse proxy.
KC_PROXY_HEADERS="xforwarded"